export declare class PureBox {
    protected constructor();
    /**
     * This is so cool.
     * @returns PureBox hello
     */
    static createBox(): PureBox;
    /**
     * Logs a message to the console
     * @param message The message to log
     */
    say(message: string): void;
}
